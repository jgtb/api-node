import Schema from '../models/schema'

import functions from '../../../functions'

const FUNCTIONS = functions(Schema, { single: 'Categoria', plural: 'Categorias', type: 'o' })

export default FUNCTIONS
